{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch manager",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            // "envFile": "${workspaceFolder}/manager/.env",
            // "args": [""]
        }
    ]
}
